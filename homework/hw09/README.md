# HW09

- Student scaffold: `hw09_sim_and_control.py`
- Shared data: `data/desired_accel.mat`, containing `q` with shape `(1001, 3)`
  and `t` with shape `(1001, 1)`
- Supporting demonstrations: `../../examples/hw09/`

The scaffold uses the course state convention `[qd, q]`.
